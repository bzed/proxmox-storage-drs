# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# Paper build intermediates (the PDF itself in docs/ is committed)
docs/.build/

# Coverage artefacts
.coverage
.coverage.*
coverage.xml
htmlcov/

# Local runtime state and secrets
state.json
config/drs.yaml
config/*.local.yaml
.env

# Throwaway dev-only diagnostics against a real cluster; never shipped source
check_pve_permissions.py
BUG_REPORT_*.md

# Claude Code local settings
.claude/

# Generated manpage (built by "make man" and by debian/rules)
man/pve-storage-drs.1

# Debian build artefacts
debian/.debhelper/
debian/files
debian/pve-storage-drs/
debian/pve-storage-drs.debhelper.log
debian/pve-storage-drs.substvars
debian/pve-storage-drs.postinst.debhelper
debian/pve-storage-drs.prerm.debhelper
debian/debhelper-build-stamp
debian/*-build-deps_*
.pybuild/
